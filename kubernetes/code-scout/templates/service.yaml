apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-scout.fullname" . }}
  labels:
    {{- include "code-scout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-scout.selectorLabels" . | nindent 4 }}
